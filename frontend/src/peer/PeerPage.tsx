import PageHeader from "../header/PageHeader";

const PeerPage = () => {
  return (
    <div id="peer-page" className="size-full">
      <PageHeader pageName="Peer" />
    </div>
  );
};

export default PeerPage;
