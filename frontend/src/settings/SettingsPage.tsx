import PageHeader from "../header/PageHeader";

const SettingsPage = () => {
  return (
    <div id="settings-page" className="size-full">
      <PageHeader pageName="Settings" />
    </div>
  );
};

export default SettingsPage;
